ubuntu上的Rough是最原始的可变精度粗糙概念格，渐进式构造，幂集是实时产生。边的更新也有部分，但不完整。
ubuntu上的Rough_small_worry是对集合产生全组合再通过筛选，没有边的更新，集合产生幂集是事先生成，直接加载的。

VBA上的Rough_small_worry和ubuntu上类似。
VBA上的Rough是经过改良的，是ubuntu上的Rough的修改版，幂集事先生成好，没有边的更新。

总得来说，渐进式和不是渐进式的区别
不是渐进式的是遍历了整个集合的幂集，但这样就不用产生子集的幂集，只要记录最终的，最大的对象集和属性集的幂集。而渐进式的没有遍历整个集合的幂集，但是需对子集产生幂集。这是受不了的。所以权衡一下，不是渐进式的方便简单。但是速度很慢。