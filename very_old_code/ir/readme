过程:
1) 数据预处理:------reuters_body.py
- 采用国际标准测试数据集reuters21578, 首先对其抽取正文, 分词, 去词干, 去停用词

- 获得reuters_clean文件夹(提取的body文件)和reuters_term文件夹(对应文件的分词之后)

2) 将所有reuters_term分成5个部分, 作为子数据库, 对于每个子数据库依次执行:-----fca.py脚本
- 构成子形式背景, 获取等价对象集(利用算法1), 生成子概念格(Godin)
- 各个节点上的子格和等价对象集存储到各自文件系统中

3) 查询keyword, 分发到各个节点上, 进行概念匹配:--------query.py
- 读取子格和等价对象集, 根据算法2进行子形式概念格匹配算法, 返回给主程序
- 根据算法3合并最终结果并返回.

