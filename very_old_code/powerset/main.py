#!/usr/bin/env python

from powerset import powerSetMaker

set_name = range(1, 21)
#f = open("output.txt", "w")
#f.writelines(str(x) + "\n" for x in powerSetMaker(set_name))
#f.close()
for x in powerSetMaker(set_name):
    pass#print x
